# Variable: ARM
# Condition: ARM ^= (Screen Failure, Not Assigned) and DM.ACTARM ^= Not Treated
# Rule: DM.USUBJID in EX.USUBJID
# verified
Authorities:
  - Organization: FDA
    Standards:
      - Name: SDTMIG
        References:
          - Citations:
              - Cited Guidance: Randomized subjects are expected to receive a
                  study treatment.
                Document: FDA
                Section: FDAB025
            Origin: FDA Business Rules
            Rule Identifier:
              Id: FB2505
              Version: '1'
            Version: '1.5'
        Version: '3.2'
Check:
  all:
    - name: ARM
      operator: is_not_contained_by_case_insensitive
      value:
        - Screen Failure
        - Not Assigned
      value_is_literal: true
    - name: ACTARM
      operator: is_not_contained_by_case_insensitive
      value:
        - Not Treated
      value_is_literal: true
    - name: USUBJID
      operator: is_not_contained_by
      value: $usubjids_in_ex
Core:
  Id: CORE-000790
  Status: Published
  Version: '1'
Description: Raise an error if a subject is assigned to an arm but the subject
  does not have a record in EX.
Executability: Partially Executable
Operations:
  - domain: EX
    id: $usubjids_in_ex
    name: USUBJID
    operator: distinct
Outcome:
  Message: All subjects assigned to an arm code (randomized) must have a record
   in EX.
  Output Variables:
    - ARM
    - ACTARM
Rule Type: Record Data
Scope:
  Classes:
    Include:
      - SPECIAL PURPOSE
  Domains:
    Include:
      - DM
Sensitivity: Record
