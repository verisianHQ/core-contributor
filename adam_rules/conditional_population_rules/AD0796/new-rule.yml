# Variable: PROJID
# Condition: PROJIDN is populated
# Rule: PROJID must be populated
# verified
Authorities:
  - Organization: CDISC
    Standards:
      - Name: ADPPKIG
        Version: '1.0'
        References:
          - Origin: ADaM Conformance Rules
            Rule Identifier:
              Id: '796'
              Version: '1'
            Version: '5.0'
            Citations:
              - Document: 'ADPPKIG v1.0'
                Section: '3.2'
                Item: 'Table 3.2'
                Cited Guidance: |-
                  ADPPKIG v1.0, Section 3.2, Table 3.2: Unique numerical representation of PROJID. The numeric versions of the primary/character variables can be assigned from its corresponding character pair variable.
Check:
  all:
    - name: PROJIDN
      operator: non_empty
    - name: PROJID
      operator: empty
Core:
  Id: AD0796
  Status: Draft
  Version: '1'
Description: 'PROJID must be populated when PROJIDN is populated.'
Executability: Fully Executable
Outcome:
  Message: 'PROJID is not populated when PROJIDN is populated.'
  Output Variables:
    - PROJID
    - PROJIDN
Rule Type: Record Data
Scope:
  Data Structures:
    Include:
      - BDS
  Domains:
    Include:
      - ALL
Sensitivity: Record
