# Variable: COHORT
# Condition: COHORTC is populated
# Rule: COHORT must be populated
# verified
Authorities:
  - Organization: CDISC
    Standards:
      - Name: ADPPKIG
        Version: '1.0'
        References:
          - Origin: ADaM Conformance Rules
            Rule Identifier:
              Id: '861'
              Version: '1'
            Version: '5.0'
            Citations:
              - Document: 'ADPPKIG v1.0'
                Section: '3.2'
                Item: 'Table 3.2'
                Cited Guidance: |-
                  ADPPKIG v1.0, Section 3.2, Table 3.2: Character representation of the COHORT variable. There must be a one-to-one mapping to COHORT. The character versions of the primary/character variables can be assigned from its corresponding numeric pair variable.
Check:
  all:
    - name: COHORTC
      operator: non_empty
    - name: COHORT
      operator: empty
Core:
  Id: AD0861
  Status: Draft
  Version: '1'
Description: 'COHORT must be populated when COHORTC is populated.'
Executability: Fully Executable
Outcome:
  Message: 'COHORT is not populated when COHORTC is populated.'
  Output Variables:
    - COHORT
    - COHORTC
Rule Type: Record Data
Scope:
  Data Structures:
    Include:
      - BDS
  Domains:
    Include:
      - ALL
Sensitivity: Record
