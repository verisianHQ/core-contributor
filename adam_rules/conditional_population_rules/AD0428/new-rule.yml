# Variable: TRTxxA
# Condition: TRTxxAN is populated
# Rule: TRTxxA must be populated
# verified
Authorities:
  - Organization: CDISC
    Standards:
      - Name: ADaMIG
        Version: '1.3'
        References:
          - Origin: ADaM Conformance Rules
            Rule Identifier:
              Id: '428'
              Version: '1'
            Version: '5.0'
            Citations:
              - Document: 'ADaMIG v1.3'
                Section: '3.2'
                Item: 'Table 3.2.4'
                Cited Guidance: |-
                  ADaMIG v1.3, Section 3.2, Table 3.2.4: TRTxxAN: Numeric representation of TRTxxA. There must be a one-to-one relationship between TRTxxAN and TRTxxA within a study. TRTxxAN cannot be present unless TRTxxA is also present. When TRTxxA and TRTxxAN are present, then on a given record, either both must be populated or both must be null.
      - Name: ADaMIG
        Version: '1.2'
        References:
          - Origin: ADaM Conformance Rules
            Rule Identifier:
              Id: '428'
              Version: '1'
            Version: '5.0'
            Citations:
              - Document: 'ADaMIG v1.2'
                Section: '3.2'
                Item: 'Table 3.2.4'
                Cited Guidance: |-
                  ADaMIG v1.2, Section 3.2, Table 3.2.4: TRTxxAN: Numeric representation of TRTxxA. There must be a one-to-one relationship between TRTxxAN and TRTxxA within a study. TRTxxAN cannot be present unless TRTxxA is also present. When TRTxxA and TRTxxAN are present, then on a given record, either both must be populated or both must be null.
Check:
  all:
    - name: '^TRT(?P<xx>0[1-9]|[1-9][0-9])AN$'
      variable_regex_pattern: true
      operator: non_empty
    - name: 'TRT{{xx}}A'
      operator: empty
Core:
  Id: AD0428
  Status: Draft
  Version: '1'
Description: TRTxxA must be populated when TRTxxAN is populated.
Executability: Fully Executable
Outcome:
  Message: TRTxxA is not populated when TRTxxAN is populated.
  Output Variables:
    - 'TRT{{xx}}A'
    - 'TRT{{xx}}AN'
Rule Type: Record Data
Scope:
  Data Structures:
    Include:
      - ADSL
  Domains:
    Include:
      - ALL
Sensitivity: Record
